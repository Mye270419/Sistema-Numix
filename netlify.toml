[build]
  command = "npm install --ignore-scripts && npm run build"
  publish = "dist/sistema-numix/browser"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200