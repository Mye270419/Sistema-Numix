[build]
  command = "npm run build"
  publish = "dist/sistema-numix/browser"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--legacy-peer-deps --prefer-offline"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
